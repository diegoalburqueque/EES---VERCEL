import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Oculta la burbuja flotante de desarrollo de Next.js (la "N" sobre la interfaz).
  devIndicators: false,
};

export default nextConfig;
